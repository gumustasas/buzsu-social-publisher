task_id: TASK-001
title: Shared Google/OpenAI Research Provider
status: READY
type: code
assigned_to: claude-code
review_by:
  - perplexity
  - chatgpt-root
depends_on: []
branch: agent/task-001-research-provider
repo: https://github.com/gumustasas/buzsu-social-publisher
goal: >
  Add one provider-independent research layer for Google Search Grounding + URL Context
  and OpenAI Web Search, with normalized sources and no change to default reel behavior.
owns:
  - src/research/**
  - test/research*
do_not_touch:
  - src/transcription/**
  - src/visual-validation/**
# shared_touch: minimum-integration files this task may edit but does not own.
# No broad refactors here — see docs/multi-agent-orchestration.md "Shared File
# Conflict Protocol". Merge order across tasks touching these is set by
# chatgpt-root.
shared_touch:
  - api/mcp.js
  - test/mcp.test.js
  - README.md
  - .env.example
acceptance:
  - research_web MCP tool exists
  - provider supports auto/google/openai without silent paid fallback
  - generate_reel_script remains researchMode=none by default
  - sources are normalized
  - npm run check passes
  - no real paid generation calls
deliverables:
  - pull_request
  - tests
  - README update
